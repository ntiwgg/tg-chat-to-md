[project]
name = "telegramanal"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "faster-whisper>=1.0",
    "tqdm>=4.0",
]

[project.scripts]
telegram-to-md = "telegram_to_md:main"

[tool.setuptools.packages.find]
include = ["src*"]
